# {{ project_name }}

Generated by **AI Circuit Architect** — a multi-agent engineering copilot for KiCad.

> ⚠️ This is an engineering **scaffold**, not a finished design. It opens in KiCad
> and lays out the project hierarchy with placeholder blocks. A human engineer
> completes the real schematic. No part values, no ERC, no production claims.

## Original request

> {{ requirements_text }}

## What is in this project

| File | What it is |
| ---- | ---------- |
| `{{ project_name }}.kicad_pro` | KiCad project file (open this in KiCad) |
| `{{ project_name }}.kicad_sch` | Root schematic with one hierarchical sheet per block |
| `sheets/*.kicad_sch` | One placeholder sheet per functional block |
| `architecture.md` | The proposed architecture (blocks, interfaces, signals, power) |
| `todo.md` | Engineering TODOs and human-review items |
| `assumptions.md` | Accepted assumptions and open questions |
| `agent_trace.json` | What each agent did (the collaboration record) |

## How it was produced

Four agents collaborated, coordinated by a stateless orchestrator:

{% for step in trace %}1. **{{ step.agent }}** ({{ step.role }}) — {{ step.summary }}
{% endfor %}

## Next steps for the engineer

1. Open `{{ project_name }}.kicad_pro` in KiCad.
2. Work through `todo.md`, especially the *needs human review* items.
3. Replace every `DUMMY_*` placeholder with a real, chosen component.
